# CLI Tool

### Display Help
```shell
./cli-tool.sh -h

Usage: cli-tool.sh [OPTIONS]
Supported commands:
  create-solution      : Create a new solution
  create-project       : Create a new project
  framework-migration  : Migrate framework version
  add-domain           : Add a new domain
  add-application-service : Add a new application service
  add-domain-service   : Add a new domain service
  add-controller       : Add a new controller
  add-endpoint         : Add a new endpoint
  add-event-publisher  : Add a new event publisher
  add-event-consumer   : Add a new event consumer
  add-command          : Add a new command
  add-query            : Add a new query
  add-acceptance-test  : Add a new acceptance test
  add-unit-test        : Add a new unit test
  add-integration-test : Add a new integration test

Options:
  -h, --help           : Display this help message
```